# AGENTS.md — bookma_close

## 作業と復帰点

明示されたユーザー指示を優先する。作業前確認・Git・証拠保存・完了手順は本書を基準とし、旧playbookの一律承認待ちより優先する。製品仕様や安全上の制約は維持する。

- 日本語で簡潔に共有する。明確な依頼は通常のプラン承認を待たず進め、重要な仮定だけ短く伝える。範囲拡大・不可逆操作・秘密情報・費用・外部公開の新たな判断は確認する。
- 編集前に `git status`・差分・ブランチを確認する。指定がなければmainを使い、既存の保存対象の未コミット差分は今回の変更と分けてチェックポイントコミットする。クリーンなら空コミット不要で、HEADを復帰点として記録する。
- チェックポイントにも秘密情報・生成物を入れず、ファイルを指定して追加する。他タスクの進行中差分や所有不明の変更は勝手にコミットせず、重ならない範囲で進める。既存の別ブランチ/worktreeは無断で切替・統合しない。Gitが無い場合は編集対象の別コピーと対応表を残す。
- 失敗時は今回の変更だけを戻す。共有済みコミットは原則revertし、他者差分や作業前の変更を巻き戻さない。reset --hard・clean・強制push・履歴書換えは明示指示なしに行わない。DB・外部サービス・Git外の素材はGitで戻らないため、変更前に個別のバックアップと復元手順を確保する。
- 完了後は関連差分とログを小さくコミットし、mainならorigin/mainへ通常pushする。既存の別ブランチはそのまま保全し、push先が明確な場合だけそこへpushする。作業前チェックポイントのpushは必須ではない。push失敗は報告し、強制上書きで解消しない。

## 証拠と保存先

- このMacの外部作業領域は `/Volumes/X9/Projects/bookma_close/`。Git管理するソース・設定・台本・必要な配布素材は本体に、大容量の未管理生成物は外部に置く。
- UI・表示変更は編集前/検証後に同じ画面・条件でキャプチャし、外部領域の `screenshots/YYYY-MM-DD/<task>/` に `<screen>-1-before.png` / `<screen>-2-after.png` として保存する。非UI変更は差分・テスト結果を証拠とし、取得不能なら理由をログに残す。撮り忘れをbeforeとして捏造しない。
- 動画/Blender素材・レンダーは `media/<production>/`、ビルド・機械ログは用途別に外部保存する。X9の実マウントを確認し、未接続なら外部出力だけ止める。本体への自動代替保存や通常フォルダとしての/Volumes/X9作成は禁止。既存の互換リンクを維持する。
- 既存のGit追跡は維持し、新規ソース・秘密情報・稼働中DBを未管理という理由だけで移さない。node_modulesと小さな判断ログは本体に残す。X9の記録は無断削除せず、再生成不能な素材は別途バックアップする。

## 記事化できる作業ログ

- 作業完了日の日本時間で `.hushchamber/logs/YYYY-MM-DD.md` にタスク単位で追記する（担当SPICA）。既存ログを書き換えて試行錯誤を消さない。
- `依頼内容`、`SPICA対応サマリー`、`調整・判断`、`未完了/次回`、`関連ファイル・検証` を基本項目とし、背景/困りごと、選択肢と採用・却下理由、失敗と復旧、結果/学びを残す。復帰点のSHA・作業ブランチ・検証コマンド/結果・before/afterの絶対パスを記録する。
- 事実・ユーザーの希望・AIの提案・未検証仮説を分け、差分一覧や会話全文の複製で済ませない。記事の切り口と未確認事項を必要時に添える。秘密情報/個人情報は記録・撮影せず、内部ログをそのまま公開しない。記事化・公開は別途依頼時に事実/権利を再確認する。

## 検証と文書

- 関連仕様と既存コードを読み、変更範囲に応じてセルフレビューと既存のlint/test/buildを実行する。文書だけなら差分・リンク・矛盾を確認し、アプリのビルド/リリースは不要。実行していない検証を成功扱いにしない。
- 仕様・画面を変えたら関連docsと画面ID一覧/構成マップを同期する。依頼外のリファクタリングや機能追加は混ぜない。完了報告は結果・検証・残件・ログ/証拠へのリンクを簡潔に示す。
